<UserControl xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="65" d:DesignWidth="200"
             x:Class="projektlabor.noah.planmeldung.uiElements.CustomInput"
             >
    <StackPanel>
        <Label FontSize="20" Content="{Binding Title}" Foreground="White"/>
        <TextBox x:Name="InputField" IsEnabled="{Binding IsWriteable}" Padding="4" FontSize="20" MaxLength="{Binding MaxLength}" Text="{Binding Text}" TextChanged="OnTypeHandler"/>
        <Label x:Name="ErrorLabel" Visibility="Collapsed" FontSize="15" Padding="5,0" Foreground="Red" FontWeight="Bold"/>
    </StackPanel>
</UserControl>
